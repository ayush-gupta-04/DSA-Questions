## Arrays Class

#### Arrays.sort(nums)
- can only have camparator if nums does not have primitives.

#### Arrays.fill(nums, -1)
- fills the array nums with -1

#### Arrays.fill(arr, 2, 5, 7);
- indices 2,3,4 become 7

#### System.out.println(Arrays.toString(arr));
#### System.out.println(Arrays.deepToString(matrix));
